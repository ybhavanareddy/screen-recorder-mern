PORT=4000
# Frontend URL for CORS (dev)
CORS_ORIGIN=http://localhost:5173

# SQLite (local dev)
SQLITE_STORAGE=./data.sqlite

# Postgres (production on Render). If set, Postgres is used:
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DB
